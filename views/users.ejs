<!DOCTYPE html>
<html>
  <head>
    <title>List users at <%=shopData.shopName%></title>
    <link rel="stylesheet" type="text/css" href="/main.css" />
  </head>
  <body>
    <h1>List users at <%=shopData.shopName%></h1>
    <h2>Here are our already existing users:</h2>
    <ul>
      <% availableUsers.forEach(function(user) { %>
      <li><%= user.username %></li>
      <% }); %>
    </ul>
  </body>
</html>
